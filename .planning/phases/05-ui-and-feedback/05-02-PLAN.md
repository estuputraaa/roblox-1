---
phase: 05-ui-and-feedback
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/StarterPlayer/StarterPlayerScripts/UIController.client.lua
  - src/ReplicatedStorage/Shared/Remotes/RemoteNames.lua
autonomous: true
must_haves:
  truths:
    - "UIController subscribe remotes HUD/event feed/ending/fail secara aman."
    - "HUD menampilkan day, cash, objective, phase, dan timer real-time."
    - "Event feed dan hint ending terbaca jelas di client tanpa crash payload invalid."
  artifacts:
    - path: src/StarterPlayer/StarterPlayerScripts/UIController.client.lua
      provides: "Client HUD controller dengan defensive remote handlers."
  key_links:
    - from: src/StarterPlayer/StarterPlayerScripts/UIController.client.lua
      to: src/ReplicatedStorage/Remotes/*
      via: "OnClientEvent listeners"
---

<objective>
Menyelesaikan layer presentasi feedback di client.

Purpose: Menjadikan data feedback server benar-benar terlihat dan bisa dipahami pemain.
Output: UIController client yang render HUD + event feed + ending/fail alerts.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-and-feedback/05-CONTEXT.md
@.planning/phases/05-ui-and-feedback/05-RESEARCH.md
@.planning/phases/05-ui-and-feedback/05-01-SUMMARY.md
@src/StarterPlayer/StarterPlayerScripts/UIController.client.lua
@src/ReplicatedStorage/Shared/Remotes/RemoteNames.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor UIController jadi reactive remote subscriber</name>
  <files>src/StarterPlayer/StarterPlayerScripts/UIController.client.lua</files>
  <action>Subscribe remote `HUDUpdate`, `DayPhaseUpdate`, `DayTimerUpdate`, `DayTransition`, `EventFeed`, `AnomalyWarning`, `FailTriggered`, `ContinuePrompt`, `EndingTriggered`. Tambahkan helper parser payload dengan default fallback agar handler robust.</action>
  <verify>UIController memiliki listener remote lengkap dengan guard `if not payload then` fallback.</verify>
  <done>Client menerima semua feedback runtime tanpa error fatal.</done>
</task>

<task type="auto">
  <name>Task 2: Render HUD fields dan objective hints</name>
  <files>src/StarterPlayer/StarterPlayerScripts/UIController.client.lua</files>
  <action>Tambah elemen HUD untuk phase dan timer, update text day/cash/objective secara real-time. Sertakan objective hint ending ringan (survive day7 + easter egg clue) pada panel objective.</action>
  <verify>UI memiliki label day/cash/objective/phase/timer dan diperbarui dari remote payload.</verify>
  <done>Pemain dapat memantau status utama game langsung dari HUD.</done>
</task>

<task type="auto">
  <name>Task 3: Implement event feed queue sederhana</name>
  <files>src/StarterPlayer/StarterPlayerScripts/UIController.client.lua</files>
  <action>Buat feed queue ringkas (mis. 4-5 pesan terakhir) dengan format level+message+time. Event kritikal (fail/ending) harus overwrite headline agar jelas terlihat.</action>
  <verify>Event feed menampilkan pesan terbaru dan tetap readable saat banyak event masuk.</verify>
  <done>UI-02 terpenuhi dengan warning/event feedback yang jelas.
</done>
</task>

</tasks>

<verification>
- [ ] Semua remote feedback tersubscribe aman di client.
- [ ] HUD update day/cash/objective/phase/timer berjalan.
- [ ] Event feed queue tampil jelas untuk warning/fail/ending.
</verification>

<success_criteria>
- UI-01 dan UI-02 terpenuhi end-to-end (server payload -> client render).
- Pemain mendapat hint ending conditions tanpa perlu membaca dokumen eksternal.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-and-feedback/05-02-SUMMARY.md`
</output>
